<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ page_title }} | SleekTech</title>

  <!-- Link to the theme styles (CSS) -->
  <link href="{{ 'theme.css' | asset_url }}" rel="stylesheet">

  <!-- Favicon (Optional) -->
  <link rel="icon" type="image/png" href="{{ 'favicon.png' | asset_url }}">

  <!-- Add any other meta tags or custom CSS links here -->
  <meta name="description" content="{{ shop.meta_description }}">

  {{ content_for_header }} <!-- Shopify's required content for headers, like meta tags and scripts -->

  <!-- Include Product Schema (from snippets) -->
  {% render 'product-schema' %} <!-- This ensures SEO-friendly structured data for products -->

</head>

<body>
  <!-- HEADER -->
  <header>
    <nav>
      <ul>
        <li><a href="{{ shop.url }}">Home</a></li>
        <li><a href="{{ shop.url }}/collections/all">Shop</a></li>
        <li><a href="{{ shop.url }}/pages/about-us">About Us</a></li>
        <li><a href="{{ shop.url }}/pages/contact">Contact</a></li>
        <li><a href="{{ shop.url }}/blogs/news">Blog</a></li>
        <li><a href="{{ shop.url }}/pages/faq">FAQ</a></li>
      </ul>
    </nav>
  </header>

  <!-- MAIN CONTENT -->
  <main>
    {% if template == 'product' %}
      <!-- Product Page -->
      {% render 'product' %}
    {% elsif template == 'collection' %}
      <!-- Collection Page -->
      {% render 'collection' %}
    {% elsif template == 'blog' %}
      <!-- Blog Page -->
      {% render 'blog' %}
    {% elsif template == 'search' %}
      <!-- Search Results Page -->
      {% render 'search' %}
    {% elsif template == 'page' %}
      <!-- Custom Static Page (e.g., About Us, Contact) -->
      {% render 'page' %}
    {% else %}
      <!-- Default Page Rendering -->
      {{ content_for_layout }}
    {% endif %}
  </main>

  <!-- FOOTER -->
  <footer>
    <p>&copy; {{ 'now' | date: '%Y' }} SleekTech. All rights reserved.</p>
    <nav>
      <ul>
        <li><a href="{{ shop.url }}/pages/about-us">About Us</a></li>
        <li><a href="{{ shop.url }}/pages/contact">Contact</a></li>
        <li><a href="{{ shop.url }}/pages/privacy-policy">Privacy Policy</a></li>
        <li><a href="{{ shop.url }}/pages/terms-of-service">Terms of Service</a></li>
      </ul>
    </nav>
  </footer>

  <!-- Scripts (JS) -->
  <script src="{{ 'theme.js' | asset_url }}"></script>

  {{ content_for_footer }} <!-- Shopify's required footer content -->
</body>

</html>
