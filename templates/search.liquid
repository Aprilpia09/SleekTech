<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Search Results | SleekTech</title>

  <!-- Link to the theme styles (CSS) -->
  <link href="{{ 'theme.css' | asset_url }}" rel="stylesheet">

  <!-- Favicon (Optional) -->
  <link rel="icon" type="image/png" href="{{ 'favicon.png' | asset_url }}">

  <!-- Add any other meta tags or custom CSS links here -->
  <meta name="description" content="Search results for your query on SleekTech">

  {{ content_for_header }} <!-- Required for Shopify's theme structure -->
</head>

<body>
  <!-- HEADER -->
  <header>
    <nav>
      <ul>
        <li><a href="{{ shop.url }}">Home</a></li>
        <li><a href="{{ shop.url }}/collections/all">Shop</a></li>
        <li><a href="{{ shop.url }}/pages/about-us">About Us</a></li>
        <li><a href="{{ shop.url }}/pages/contact">Contact</a></li>
        <li><a href="{{ shop.url }}/blogs/news">Blog</a></li>
        <li><a href="{{ shop.url }}/pages/faq">FAQ</a></li>
      </ul>
    </nav>
  </header>

  <!-- MAIN CONTENT -->
  <main>
    <h1>Search Results for "{{ search.terms }}"</h1>

    {% if search.results.size > 0 %}
      <ul class="search-results">
        {% for result in search.results %}
          <li>
            <a href="{{ result.url }}">
              <h2>{{ result.title }}</h2>
              <p>{{ result.excerpt }}</p>
            </a>
          </li>
        {% endfor %}
      </ul>
    {% else %}
      <p>No results found for "{{ search.terms }}". Please try a different search term.</p>
    {% endif %}
  </main>

  <!-- FOOTER -->
  <footer>
    <p>&copy; {{ 'now' | date: '%Y' }} SleekTech. All rights reserved.</p>
    <nav>
      <ul>
        <li><a href="{{ shop.url }}/pages/about-us">About Us</a></li>
        <li><a href="{{ shop.url }}/pages/contact">Contact</a></li>
        <li><a href="{{ shop.url }}/pages/privacy-policy">Privacy Policy</a></li>
        <li><a href="{{ shop.url }}/pages/terms-of-service">Terms of Service</a></li>
      </ul>
    </nav>
  </footer>

  <!-- Scripts (JS) -->
  <script src="{{ 'theme.js' | asset_url }}"></script>

  {{ content_for_footer }} <!-- Required for Shopify's theme structure -->
</body>

</html>
